import React from 'react'
import ReactDOM from 'react-dom'
import { BrowserRouter, Switch, Route } from 'react-router-dom'
import render from '../components/renderElement'
<% mdImports.forEach(function (item) { %>
  import <%= item.name %> from "<%= item.path %>"
<% }) %>
<% cssFiles.forEach(function (filePath) { %>
import "<%= filePath %>"
<% }) %>


const renderFunctions = <%- renderElementFuntions %>
function App(){
  return (
    <div>
      <%- getRoute({ "routes": routes, "getRoute": getRoute }) %>
    </div>
  )
}

ReactDOM.render(
<BrowserRouter>
  <App />
</BrowserRouter>,
document.getElementById('app')
);
